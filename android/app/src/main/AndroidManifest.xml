<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Playground">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".BasicUIActivity"
            android:exported="false"
            android:label="Basic UI"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".WebViewActivity"
            android:exported="true"
            android:label="Web View"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".PreferencesActivity"
            android:exported="false"
            android:label="Preferences"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".ContinuousAnimationActivity"
            android:exported="false"
            android:label="Continuous Animation"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".PermissionsActivity"
            android:exported="false"
            android:label="Permissions and Alerts"
            android:parentActivityName=".MainActivity" />
    </application>

</manifest>
